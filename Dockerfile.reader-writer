FROM golang:1.23-alpine AS build
WORKDIR /src
COPY . .
RUN CGO_ENABLED=0 go build -o /out/reader-writer ./cmd/reader-writer

FROM gcr.io/distroless/base-debian12:nonroot
WORKDIR /
COPY --from=build /out/reader-writer /reader-writer
USER nonroot:nonroot
ENTRYPOINT ["/reader-writer"]
